1.5:
- docker run -it --name testiubuntu ubuntu:16.04 sh -c 
//$website;'
- The testiubuntu container start through VS Code terminal so I started it in Docker Desktop
- docker exec -it testiubuntu bash
- apt-get update
- apt install -y curl
- echo "Input website:"; read website; echo "Searching.."; sleep 1; curl http://$website;

